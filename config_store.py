# config_store.py

# Global variable to store user-submitted config
# Mock config to bypass config check
db_config = {
    "host": "mock",
    "port": 1521,
    "service": "mock",
    "user": "mock",
    "password": "mock"
}
